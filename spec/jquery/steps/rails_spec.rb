require 'spec_helper'

describe Jquery::Steps::Rails do
  it 'has a version number' do
    expect(Jquery::Steps::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
